[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[invariant]
runs           = 1000   # up from default 256
depth          = 200    # up from default 500 — more calls per sequence  
fail_on_revert = false